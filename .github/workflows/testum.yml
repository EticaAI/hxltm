# systēma:
#  - https://en.wiktionary.org/wiki/systema#Latin

# SPDX-License-Identifier: Unlicense OR 0BSD
# .github/workflows/hxltm-normam-documentum.yml

# Trivia:
# - "HXLTM", https://hxltm.etica.ai/
# - "computum",https://en.wiktionary.org/wiki/computus#Latin
# - "referēns", https://en.wiktionary.org/wiki/referens#Latin
# - "testum", https://en.wiktionary.org/wiki/testum#Latin

# https://en.wiktionary.org/wiki/computus

name: hxltm-computum-referens

# name: testum

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # python-version: [3.7, 3.8, 3.9, 3.10]
        python-version: [3.7, 3.8, 3.9]

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tox tox-gh-actions

    - name: Test with tox
      run: tox

      # TODO: remove the continue-on-error
      continue-on-error: true